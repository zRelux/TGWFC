export interface ColorsInterface {
  primary: string;
  secondary: string;
  tertiary: string;
  primaryText: string;
}

const colors: ColorsInterface = {
  primary: '#414141',
  secondary: '#5d5d5d',
  tertiary: '#838383',
  primaryText: '#f2f2f2'
};

export default colors;
